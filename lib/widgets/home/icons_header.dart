import 'package:flutter/material.dart';

class IconsHeader extends StatelessWidget {
  const IconsHeader({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Icons Header");
  }
}
